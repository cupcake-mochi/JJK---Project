# MEDIDA — para onde vão as seis, e a `Dupla` não ficou órfã: ela ERA o defeito

*10/09/2026. **Conta rodada: `04-fase-1/fila/medir-a-dupla.py`** — o 27º.
Continuação do `MEDIDA-as-seis-prontas.md`, que mediu que `6` de `6` estão na escada morta.*

> ## ⟹ `3` dos `4` degraus mortos têm herdeiro EXATO. O quarto foi morto de propósito, e está escrito.

---

# § 1 · O CRUZAMENTO — e a leitura que estava publicada está ERRADA

**Cruzado por IDENTIDADE nos três eixos — personagens, fator e ações —, lendo as duas escadas nos
documentos donos** *(peça 26 §4 · `RASCUNHO-5` Passo 1)*.

| a MORTA | pessoas · fator · ações | ⟹ o HERDEIRO | conferência célula a célula |
|---|---|---|---|
| **`Ronda`** | `1` · `0,25` · `1` | ### **`Ameaça`** | ✅ **`29`/`29`** em vida, dano, ações **e golpe** |
| **`Dupla`** | `2` · `0,50` · `1` | ### ⚠ **— ÓRFÃ —** | — |
| **`Alcateia`** | `4` · `1,00` · `3` | ### **`Desastre`** | ✅ **`29`/`29`** |
| **`Calamidade`** *(morta)* | `6` · `1,50` · `5` | ### **`Catástrofe`** | ✅ **`29`/`29`** |

**Não é "parecido". É a MESMA ficha com outro nome:** *as duas tabelas publicadas da peça 26 — a `§4.1`
(vida · dano no nv`10`/`20`/`30`) e a `§4.4` (por rodada · ações · golpe no nv`30`) — batem célula por
célula com a coluna viva da `TABELA.md`.* **O `8d8 + 37` da `Alcateia` é o `8d8 + 37` do `Desastre`.**

## ⚠⚠ A CORREÇÃO — `Alcateia ⟹ Capanga` não fecha, e erra por MUITO

***O que o `ESTADO-onde-paramos.md` e a `MEDIDA-as-seis-prontas.md` §4 publicam:*** *"`Ronda` era `1`
personagem ⟹ `Ameaça`; **`Alcateia` era o esquadrão ⟹ `Capanga`**"*.

| | `Alcateia` | `Capanga` | `Desastre` |
|---|---|---|---|
| fator | `1,00` | `0,25` | **`1,00`** |
| ações | `3` | `1` | **`3`** |
| corpos | `1` | `8` | **`1`** |
| vida no nv`20` | `660` | `55` | **`660`** |

> ### `Alcateia` ÷ `Capanga` em vida: **`12,00×`**. `Alcateia` ÷ `Desastre`: **`1,00×`**.
> **A `Alcateia` é a linha do manual sem tocar em nada** — *a própria peça 26 §4 escreve isso.* **E o
> `Desastre` também é.** *Os dois são a mesma coisa.*

**E a peça 26 §4.3 já separava os dois, palavra por palavra:**

> *"**É por isso que o capanga do manual não é uma `Ronda`.** A `Ronda` é um quarto do chefe nos dois
> eixos […] e o capanga é um quarto da vida com um TERÇO do dano."*

⟹ **O capanga do manual era EIXO SEPARADO da escada, não um degrau dela.** *Ele morava nas colunas
`capanga vida` / `capanga dano` das `FAIXAS`, ao lado das quatro categorias — não dentro delas.*

> ### ⚠ Se o commit sair com `Alcateia ⟹ Capanga`, o `Tsuchigumo` e o `Oni` viram `1/12` do que são.
> *E o `Oni` é a ficha cuja linha diz "**o grupo do nível 6 precisa dos quatro para segurar**".*

## ⚠⚠ E A ARMADILHA QUE NÃO ESTAVA EM LISTA NENHUMA — o nome `Calamidade`

| | `Calamidade` MORTA | `Calamidade` VIVA |
|---|---|---|
| personagens | `6` | **`8`** |
| fator | `1,50` | ### **`2,00`** |
| ações | `5` | `6` |

> ### O mesmo nome vale **`1,33×` mais** na escada viva. E o herdeiro real da morta é a `Catástrofe`.
> **Nenhuma das seis usa `Calamidade`** — *mas o `make.js` imprime a escada INTEIRA em duas tabelas do
> `.docx`, não só as seis fichas.* **A COLUNA dela vai pro livro do mesmo jeito.**
>
> ⟹ *Renomear as seis e deixar o `Calamidade` quieto é a armadilha: um mestre lê `Calamidade` na
> tabela do bloco e `Calamidade` na peça 26 nova, e são dois bichos diferentes.*

---

# § 2 · A `Dupla` — ela não ficou órfã por descuido. Ela foi MORTA, e está escrito em DOIS arquivos

**O repositório respondeu antes da pesquisa pela sexta vez.** *A resposta estava em casa, em dois
arquivos que ninguém tinha cruzado com as seis.*

| onde | o que já está escrito |
|---|---|
| **`a-escada-com-numero.md`** | *"No bestiário antigo ele saía de `personagens − 1`, e a razão `pessoas ÷ (pessoas − 1)` explodia embaixo — `2÷1`, `4÷3`, `6÷5`. **Foi isso que quebrou a `Dupla`**, que levava o dobro do orçamento pela mesma porta."* |
| **`a-escada-com-numero.md`** | *"A do bestiário antigo era de `23%` a `45%`, vinte e dois pontos, e **o topo dela era a `Dupla`**."* |
| **`A-TABELA-pontos-por-acao.md`** | *"A `Dupla` era o pico — `24,2` pontos, contra o teto de `24` do jogador. **Ela MORREU.**"* |

## E a medição confirma, nos `29` níveis

*A métrica é a que a própria escada chama de **"a que pegou a `Dupla`"**: que fatia da vida de UM
personagem um golpe leva. Âncoras: a fatia publicada do `Desastre` (`30%`) e o `0,923` da `Intervenção`.*

| | nv`2` | nv`10` | nv`20` | nv`30` | fora da banda |
|---|---|---|---|---|---|
| **a `Dupla` publicada** — fator `0,50` · `1` ação | `43,6%` | `44,4%` | `44,7%` | ### **`44,8%`** | ### ⚠ **`29` de `29`** |
| `Ameaça` *(o piso vivo)* | `21,8%` | `22,8%` | `22,7%` | `22,6%` | ✅ `0` de `29` |
| `Desastre` *(a âncora, com a Intervenção)* | `27,7%` | `27,7%` | `27,7%` | `27,7%` | ✅ `0` de `29` |

> ### A `Dupla` entrega `45%` da vida de um personagem NUM GOLPE SÓ, contra um teto vivo de `28%`.
> **`1,60×` o topo da banda, em `29` de `29` níveis.** *E as duas fichas do livro — `Kamaitachi` e
> `Kitsune` — carregam isso hoje.*
>
> ⚡ *E os `21,8%` e os `27,7%` recomputados aqui são **exatamente** o piso e o topo que o
> `DECIDIDO-o-capanga.md` §1 publica.* **A varredura reproduz a banda do dono.**

---

# § 3 · AS SAÍDAS, MEDIDAS

**O simulador é o do item `9`, generalizado pra N corpos.** *Antes de qualquer conclusão ele reproduz
DUAS âncoras que não calibrou:*

| a guarda | medido | publicado |
|---|---|---|
| `1 Desastre` sozinho cobra | **`67,5%`** | `68%` *(peça 26 §4.5)* |
| e exige | **`4,00` pessoas** | `4` |
| `4 Ronda` contra `1 Alcateia` | **`0,750×`** | `0,75×`–`0,77×` *(peça 26 §4.3)* |

## A tabela das saídas

| a saída | cobra da mesa padrão | vs a `Dupla` | corpos |
|---|---|---|---|
| **a `Dupla` de hoje** *(o que está no livro)* | `22,5%` | — | `1` |
| **`A`** · duas `Ameaça` | `16,9%` | **`−25,0%`** | `2` |
| **`B`** · `Ameaça` + `3 Capanga` *(a régua do item `9`)* | `28,2%` | `+25,2%` | `4` |
| **`C`** · promover a `Desastre` | `67,5%` | ### `+200,0%` | `1` |
| **`D`** · rebaixar a `Ameaça` | `5,6%` | `−75,0%` | `1` |
| **`E`** · um corpo fator `0,50` com **`2` ações** | `22,5%` | ### **`0,0%`** | `1` |

> ### ⚡ A saída `A` erra `−25,0%` — e é o MESMO `0,75×` que o §4.3 publica pra quatro `Ronda`.
> *"Eles morrem em fila e a saída deles despenca."* **Dois corpos de `0,25` não valem um de `0,50`,
> pela mesma razão que quatro não valem um inteiro.** *Não é achado novo: é a mesma propriedade,
> medida no degrau de baixo.*

> ### ⚡ E a saída `E` sai de graça, porque `Ações` virou número DECLARADO.
> **A escada viva escreve: *"E `Ações` é número DECLARADO"*** — *a regra `personagens − 1` está morta.*
> **Então o mesmo fator `0,50` com `2` ações em vez de `1` cai pra `21,8%`–`22,4%` — DENTRO da banda.**
> *Vida e dano por rodada não se movem ⟹ o encontro é idêntico.* **O golpe do nv`30` sai de
> `8d12 + 57` pra `5d10 + 27`.**
>
> ⚠ **Mas ela ressuscita um degrau que o projeto fechou DUAS vezes de propósito.** *E precisa de nome.*

## ⚠ E uma coluna que NÃO serve pra decidir aqui: `pessoas exigidas`

*Ela devolve `2,00` pra `Dupla` **e** `1,99` pra duas `Ameaça` — o que faria parecer que são iguais.*

| numa mesa de | a `Dupla` cobra | duas `Ameaça` cobram |
|---|---|---|
| `1,90` pessoas | `94,7%` | `71,1%` |
| `2,00` pessoas | `67,5%` | `56,2%` |

> **De `1,90` pra `2,00` a cobrança despenca porque a luta perde uma RODADA inteira**, e a busca
> binária cai dentro do degrau. ⚠ *É o mesmo defeito que o `DECIDIDO-o-capanga-unico` §2 já registrou:
> **"o chefe a `91,5%` cobra `67,4%`; a `92%` cobra `80,3%`"** — meio ponto move treze.*
> ### ⟹ A unidade que decide é `cobra da mesa padrão`. A de `pessoas` fica pra conferência.

---

# § 4 · ⚠⚠⚠ O NÓ DE VERDADE — a `Kitsune` CONJURA, e nada na escada viva conjura ali

**O `dados.js` publica, na ficha dela:** *"uma técnica de `Classe 1`, escrita pelo mestre no orçamento
de **`4,2` pontos** da ação."* **E a linha dela é: *"A única da faixa que conjura."***

*O piso do `RASCUNHO-5`: golpe da ação ÷ `4,5` abaixo de `3` pontos ⟹ ela **não monta feitiço nenhum**.*

| a saída, na faixa `5 a 8` | pontos por ação | conjura? |
|---|---|---|
| **a `Dupla` de hoje** | **`4,2`** | ### ✅ **sim** |
| `A` · duas `Ameaça` | `2,2` | ❌ **seco** |
| `B` · `Ameaça` + `3 Capanga` | `2,2` | ❌ **seco** |
| `C` · promover a `Desastre` | `2,9` | ❌ **seco** |
| `D` · rebaixar a `Ameaça` | `2,2` | ❌ **seco** |
| `E` · fator `0,50` com `2` ações | `2,1` | ❌ **seco** |
| **o TOPO da escada viva na mesma faixa — `Calamidade`** | `2,9` | ### ❌ **seco** |

> ### `0` de `6`. **Nem o topo da escada viva conjura no nível `5` ao `8`.**
> *E isso não é defeito — é o piso `seco`, que ele mesmo fechou no item `21` em 10/09.* **A `A-TABELA`
> publica a linha do nv`5` inteira como `seco`, nas cinco categorias.**

> ## ⟹ A única coisa que faz a `Kitsune` ser a `Kitsune` só existe na escada MORTA.
> **Ela conjura porque a `Dupla` concentrava `2` pessoas de orçamento numa ação só** — *que é
> exatamente o defeito que a escada nova removeu de propósito.*
> ### Consertar a categoria dela apaga a ficha. Manter a ficha ressuscita o defeito.

## ⚡ MAS EXISTE UMA TERCEIRA PORTA, e ela é de mudar a FAIXA, não a categoria

| a `Ameaça`, por faixa | pontos | conjura? | vs os `4,2` dela |
|---|---|---|---|
| nv `2 a 4` | `0,9` | ❌ seco | `−78,8%` |
| nv `5 a 8` *(onde ela está)* | `2,2` | ❌ seco | `−47,1%` |
| ### nv `9 a 12` | ### **`4,2`** | ### ✅ **sim** | ### **`+0,5%`** |
| nv `13 a 16` | `6,2` | ✅ sim | `+48,1%` |

> ### **SAÍDA `F`** · a `Kitsune` sobe de faixa — `5 a 8` ⟹ `9 a 12` —, e vira `Ameaça`.
> **Ela fica com `4,2` pontos, que são EXATAMENTE os `4,2` que a ficha publicada já declara.**
> ⟹ *a característica dela — "no orçamento de `4,2` pontos da ação" — **não muda uma letra**.*
>
> ⚠ **O preço:** *as seis deixam de cobrir `nv 2 ao 6` e passam a cobrir `nv 2 ao 12`.*

---

# § 5 · E A GRADE QUE PRODUZIU O NÚMERO SEIS — ela ainda dá seis

**O `dados.js` deriva o número, e não escolhe:** *"a faixa do nível `2` ao `6` cruza DUAS linhas de
`FAIXAS` com as QUATRO categorias — oito células —, e a `Calamidade` exige seis feiticeiros, que um
grupo dessa faixa não tem."*

**Rodando a MESMA derivação na escada viva:**

| | |
|---|---|
| faixas que cobrem o nv `2` ao `6` | **`2`** — `2 a 4` e `5 a 8` |
| categorias que uma mesa de `4` aguenta | **`3`** — `Capanga` · `Ameaça` · `Desastre` |
| categorias sem gente pra elas | `2` — `Catástrofe` (`6p`) · `Calamidade` (`8p`) |

> ### ⚡ `2 × 3` = **`6` células**. A grade viva dá o MESMO número.
> **As seis continuam sendo seis.** *O que muda é a COMPOSIÇÃO: sai `Ronda`·`Dupla`·`Alcateia`, entra
> `Capanga`·`Ameaça`·`Desastre` — e **a vaga da `Dupla` vira vaga de `Capanga`**.*

**E o que essa vaga passa a custar, medido:**

| | cobra da mesa padrão | vs a `Dupla` |
|---|---|---|
| `2` `Capanga` sozinhos | `11,3%` | `−49,8%` |
| `3` `Capanga` sozinhos | `16,9%` | `−24,8%` |
| **`8` `Capanga`** — o esquadrão cheio | `67,7%` | `+201,0%` |

⚠ **E o `Capanga` é o único degrau vivo cuja ficha NÃO sai de multiplicar a linha do manual** — *a vida
dele é `dano do grupo ÷ 4`.* **Ele não se gera pelo mesmo caminho das outras cinco no `make.js`.**

---

# ⟹ § 6 · A RECOMENDAÇÃO, e onde está o martelo dele

## Quatro das seis são execução pura — zero decisão

| ficha | de | ⟹ para | o que muda |
|---|---|---|---|
| `Betobeto` | `Ronda` | **`Ameaça`** | **só o rótulo** |
| `Hitotsume` | `Ronda` | **`Ameaça`** | **só o rótulo** |
| `Tsuchigumo` | `Alcateia` | **`Desastre`** | **só o rótulo** |
| `Oni` | `Alcateia` | **`Desastre`** | **só o rótulo** |

*Nenhum número se move em nenhuma das quatro. A conferência deu `29`/`29` nas quatro colunas.*

## E duas pedem martelo — e são pedidos diferentes

### `Kamaitachi` — recomendação `A`, e ela é barata

**A ficção dela JÁ É dois corpos:** *"Duas comadres-de-foice que trabalham juntas: uma derruba, a outra
corta."* **A característica dela: *"nenhuma — as duas atacam no mesmo turno, e isso já é a categoria".***

> ⟹ **Duas `Ameaça`.** *A ficção não muda uma palavra, e o golpe entra na banda.* **Custa `−25,0%` de
> encontro** — *o mesmo `0,75×` que o §4.3 já cobra de quatro corpos repartidos.*
>
> ⚠ **A alternativa da grade é `Capanga`**, e ela fecha a célula que o §5 diz que existe — *mas pede
> editar a ficção de "duas" pra "um bando", porque o `Capanga` é esquadrão de `8`.*

### `Kitsune` — aqui é escolha de SABOR, e é dele

**As duas portas, com a conta:**

| | o que ela custa |
|---|---|
| **`F`** · sobe pra `9 a 12` e vira `Ameaça` | **a ficha inteira sobrevive, letra por letra** — *`4,2` pontos, o mesmo número já publicado.* ⚠ *Custa a promessa "as seis cobrem o nv `2` ao `6`"* |
| **`A`/`D`** · fica em `5 a 8` e vira `Ameaça` | **a mesa não muda de nível** — ⚠ *mas ela perde a técnica e vira bloco `seco`.* **A linha "a única da faixa que conjura" sai do livro** |

> ### A minha leitura: a `F`.
> **O piso `seco` foi fechado com a conta pronta** *(item `21`: `25,2%` do D&D `CR 0–1` e `45,7%` dos
> minions do Draw Steel têm uma entrada só)*. **Ele é regra boa, e ela diz que nv`5` não conjura.**
> *Brigar com ele pra salvar uma ficha reabre o que já foi medido.*
>
> **A `F` respeita o piso E salva a ficha, e o preço dela é só de COBERTURA** — *e cobertura é escolha
> de escopo, não defeito de máquina.*

---

# § 7 · O QUE ISSO PÕE NA LISTA DO `Claude 2` — e a varredura deu `24` linhas

> ### ⚠ Nada disto é trabalho deste projeto. Outra conta escreve lá.
> **E nada disto está na `sobrecarga/MEXIDAS-no-repositorio.md`** — *ela cobre a peça 26 (`11` +
> validador) e o gerador de FEITIÇO (`3`). O `gerador-inimigo/` não é nenhum dos dois.*

**Varrido pelo `§9` do script, procurando os quatro nomes mortos lidos da própria peça 26:**

| arquivo | linhas que falam a escada morta |
|---|---|
| `gerador-inimigo/dados.js` | **`13`** |
| `gerador-inimigo/make.js` | **`11`** |
| ### total | ### **`24`** |

## O que muda em cada um

| # | onde | o quê |
|---|---|---|
| **`1`** | `dados.js` `L24-27`, `CATEGORIAS` | os quatro degraus ⟹ os cinco vivos. ⚠ **`Calamidade` NÃO é renomeação: ela vira `Catástrofe`, e uma `Calamidade` nova entra por cima** |
| **`2`** | `dados.js` `L83·93·98·108`, `PRONTAS` | `Betobeto` e `Hitotsume` ⟹ `Ameaça` · `Tsuchigumo` e `Oni` ⟹ `Desastre` |
| **`3`** | `dados.js` `L88·103`, `PRONTAS` | ⚠ `Kamaitachi` e `Kitsune` — **depende do martelo do §6** |
| **`4`** | `dados.js` `L21`, comentário | *"As quatro categorias da peça 26"* ⟹ **cinco** |
| **`5`** | `dados.js` `L117`, `CAMBIO` | o `4` ⟹ **`8`** — *é a mesma checagem `5` que já está vermelha* |
| **`6`** | `dados.js` `L69` + `L115`, `RONDA_CONTRA_ALCATEIA` | o `[0.75, 0.77]` é da escada morta — **ou sai, ou vira a medição nova** |
| **`7`** | `dados.js` `L77`, a derivação do seis | *"as QUATRO categorias… oito células"* ⟹ **`2` faixas × `3` categorias = `6`** *(o §5 acima)* |
| **`8`** | `dados.js` `L65`, `SUBCATEGORIAS` | `68`/`58`/`56`/`62` ⟹ a tabela nova do §4.5 — `100%` · `91,5%` · `83,0%` · `74,5%` |
| **`9`** | `make.js` `L111`, a instrução ao mestre | *"`Ronda` é um, `Dupla` é dois, `Alcateia` é quatro, `Calamidade` é seis"* — ⚠ **é a frase que ensina a escada, e as quatro estão mortas** |
| **`10`** | `make.js` `L115` + `L260`, o exemplo | *"Isso é uma `Alcateia`: `475` de vida"* ⟹ **`Desastre`** |
| **`11`** | `make.js` `L131` + `L133`, a nota do golpe | *"a `Dupla` bate mais forte que a `Calamidade`"* — **as duas sumiram da frase** |
| **`12`** | `make.js` `L150` | *"Um chefe de `Alcateia` vale `${CAMBIO}` capangas"* — ⚠ **o câmbio é `8`, e a frase da `Ronda` é da escada morta** |
| **`13`** | `make.js` `L176` | *"não existe degrau acima da `Calamidade`"* — ⚠⚠ **é literalmente a frase que o item `5` matou por medição** |
| **`14`** | `make.js` `L281`, a derivação do seis | a mesma do item `7`, no texto impresso |
| **`15`** | `make.js` `L318` | *"A `Calamidade` da faixa fica de fora porque exige seis feiticeiros"* — ⚠ **na escada viva quem exige seis é a `Catástrofe`; a `Calamidade` exige `8`.** *A frase continua verdadeira e passa a apontar pro bicho errado* |
| **`16`** | `make.js` `L8` · `L100` | comentários de cabeçalho |
| **`17`** | `conferir-ficha.py`, bloco `7` | ele compara o `dados.js` com a peça 26 — **acende junto** |
| **`18`** | regenerar | o `bloco-de-inimigo.docx` pelo `make.js` |

> ### ⚠⚠ E o `13` é a sétima vez que a mesma frase morta reaparece num lugar novo.
> ***O item `5` mediu e fechou:*** *"a escada não tem o que vender acima da `Calamidade`" saiu, porque
> **a categoria mede pessoas e o número existe fora da escada**.* **A `MEXIDAS-no-repositorio.md` marcou
> a cópia da peça 26. Ninguém tinha visto a cópia do `make.js`** — *e é essa que vai impressa pro livro.*

> ### ⚠ E o que morde de verdade: o `make.js` computa TUDO de `FAIXAS × CATEGORIAS`.
> *A escada viva quebra essa forma em um ponto — o `Capanga`.* **A vida dele é `dano do grupo ÷ 4`, e
> não `fator × a linha do manual`.** *Enquanto o `dados.js` não tiver de onde tirar isso, a coluna
> `Capanga` não sai da mesma fábrica que as outras quatro.* ### É a única mexida que é código, e não texto.

---

## De onde saiu

**`medir-a-dupla.py`** *(este projeto, o 27º)* · **peça 26 §4 · §4.1 · §4.3 · §4.4 · §4.5**
*(a escada morta e as duas tabelas publicadas)* · **`a-escada-com-numero.md`** *(o registro de que a
`Dupla` foi morta)* · **`A-TABELA-pontos-por-acao.md`** *(o `24,2` contra o teto `24`)* ·
**`DECIDIDO-o-capanga.md` §1** *(a banda `21%`–`28%`)* · **`DECIDIDO-o-degrau.md`** *(o fator contínuo)*
· **`DECIDIDO-o-capanga-unico.md`** *(a régua `0,083`)* · **`TABELA.md`** *(a escada viva, `29` níveis)*
· **`gerador-inimigo/dados.js`** *(o dono das seis)*
